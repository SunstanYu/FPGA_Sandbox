📄 Sandbox UI & Architecture Improvement Notes

🔴 1. 关键问题：交互反馈不足

当前 UI 存在以下问题：

材料选中状态不明显
鼠标指示过小
点击无视觉反馈
sandbox 区域缺乏可操作提示
✅ 1.1 材料选中高亮（必须优化）
当前问题

仅使用边框高亮，辨识度低

建议方案
使用背景高亮（推荐）
或增加填充色 overlay
或增加 indicator（如小标记）

示例：

[ WALL ]   (灰)
[ WATER ]  (灰)
[ SAND ]   (黄色背景高亮)
✅ 1.2 鼠标指示优化（必须优化）
当前问题

单个像素点（不明显）

建议方案

改为：

圆形 brush（推荐）
或十字准星

建议参数：

外圈：白色
内圈：黄色
半径：4~6 pixels
✅ 1.3 点击反馈（必须优化）
当前问题

点击操作无反馈

建议方案
paint 区域短暂高亮（1~2帧）
或颜色增强一帧

简单实现：

写入像素时使用更亮颜色 → 下一帧恢复
🟡 2. UI布局优化
✔ 2.1 分割线优化
当前问题

蓝色粗线视觉过重

建议
改为更细线条
或使用更暗颜色
或减少分割线，增加间距
✔ 2.2 字体对齐
当前问题

文字偏左，不居中

建议
水平居中对齐
垂直略上偏（视觉平衡）
✔ 2.3 增加材料颜色预览

当前仅文字，不直观

建议：
[SAND]
[■■■■]  ← 黄色块

优点：

用户无需记忆颜色
更接近实际沙盒游戏 UI
🟢 3. Sandbox区域优化
✔ 3.1 背景优化（强烈建议）
当前问题

纯黑背景，缺乏空间感

建议
使用深灰背景代替黑色

🔵 4. 状态信息显示（加分项）
当前问题

无系统状态显示

建议添加：
Material: SAND | Brush: 4x4

位置：

屏幕左上角

实现：

HPS 使用 VGA_text() 即可